version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd apps/web && npm install
    build:
      commands:
        - cd apps/web && npm run build
  artifacts:
    baseDirectory: apps/web/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - apps/web/node_modules/**/*
