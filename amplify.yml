version: 1
applications:
  - appRoot: apps/web
    frontend:
      phases:
        preBuild:
          commands:
            - npm install
        build:
          commands:
            - npm run build
      artifacts:
        baseDirectory: apps/web/.amplify-hosting
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
